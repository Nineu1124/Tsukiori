# Spike Reports

Each top-level Spike writes one sanitized report here. Reports identify Checkpoints as task ID plus ordinal, for example T0.1-C1, and link to committed fixtures without containing credentials or user source code.
