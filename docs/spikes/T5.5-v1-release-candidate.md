# T5.5 Windows V1 Release Candidate 回归报告

- 任务：T5.5
- Release Candidate：1.0.0-rc.1
- 评估日期：2026-08-02
- 平台：Windows Native x64
- 结论：通过
- 兼容性矩阵：`tests/fixtures/release/v1.0.0-rc.1-compatibility.json`
- 脱敏结果：`tests/fixtures/release/t5.5-result.json`
- 已知问题：`docs/releases/V1.0.0-rc.1-known-issues.md`

## 总结

Desktop 与 Daemon 均提升为 `1.0.0-rc.1`，继续通过 Host Protocol 1 与 IPC Protocol 1 建立显式兼容边界。RC 锁定 OpenCode 1.18.4 OpenAPI Hash、Codex 0.146.0 非实验 Schema Hash 和两端 knownIssues；超出锁定范围的 Runtime 仍分别为 `incompatible_older` 或 `unverified_newer`，不会从假设提升为已支持。

本机从源码真实生成 assisted NSIS，完成安装、packaged Desktop/Daemon Smoke、更新覆盖、卸载、重新安装和最终卸载。上一测试数据库 Schema 5 的 Session、Turn、Worktree、Workspace Binding 与磁盘标记在升级到 Schema 6 后全部恢复。公共 Windows CI 新增相同的临时目录 NSIS 生命周期脚本；安装包与签名材料不上传、不提交。

## T5.5-C1 Windows x64 全回归

要求的回归集合：

- Adapter Contract：4/4；
- Git/Diff/Merge：14/14；
- Recovery：3/3；
- Security/Credential：6/6；
- OpenCode Alpha E2E：5/5；
- 双 Runtime E2E：2/2；
- Release Manager：5/5；
- Release Candidate：4/4；
- TypeScript typecheck：32/32。

Windows CI 工作流继续运行全部既有无凭据测试，并新增 `test:release-candidate` 与 `verify-windows-release-candidate.ps1`。T5.4 基线 CI `30749602137` 已验证 Adapter Contract、Git、Recovery、安全和 E2E 集合；包含 RC 版本与真实 Installer Lifecycle 的本提交 CI 必须成功后才允许开始 G5。

结果：通过；后续 Gate 条件：本提交 Windows CI 成功。

## T5.5-C2 Runtime 兼容性锁定

| Runtime | 最低/最高已测版本 | Schema Hash | knownIssues |
| --- | --- | --- | ---: |
| OpenCode | 1.18.4 / 1.18.4 | `f5cb443f0d160fc4b17190f64c2401f199160eb2137ce4e00ca319b99aa34005` | 3 |
| Codex | 0.146.0 / 0.146.0 | `776ab888b2e673311a4c53ed086d66b061dcfdc8ca595b4cf43220c2b3d04368` | 3 |

测试从原始 OpenAPI/Schema Manifest 与 Capability Matrix 重新比对版本、Hash 和 knownIssues，避免 RC Fixture 与验证来源漂移。Codex 0.142.5 继续明确为不兼容旧版本；OpenCode 运行中 Turn 崩溃恢复、Codex structured network approval 继续保持 `unknown`。

结果：通过。

## T5.5-C3 上一版本升级恢复

测试先用 `targetVersion:5` 创建上一测试版本状态：Execution Environment、Project、Session、Turn、独立 Worktree、Workspace Binding 和磁盘恢复标记。随后用 RC 默认 Schema 6 打开：

- Migration 前生成 SQLite 一致性备份；
- Session 标题与 Lifecycle/Activity/Health 可读；
- Turn 历史数量仍为 1；
- Worktree 路径与 Workspace Binding 的 last-known commit 不变；
- Worktree 磁盘标记内容不变；
- Migration 列表完整为 1–6。

结果：通过。

## T5.5-C4 干净 Windows 安装、更新、恢复与回滚

`verify-windows-release-candidate.ps1` 只使用新建系统临时目录：

1. 构建 Daemon、Desktop 并暂存 packaged Daemon；
2. 用 electron-builder 26.15.7 生成 Windows x64 assisted NSIS；
3. 静默安装 RC 并运行 packaged Smoke；
4. 再次安装同一 RC 验证更新覆盖；
5. 卸载、重装、最终卸载并确认用户数据保留；
6. 不上传 Artifact，不持久化 Credential 或签名材料。

本机实测 Artifact：214,324,819 bytes，SHA-256 `e50c880cdc729a94084fe84f92041779a4459974997fbd50a53f671df06756f0`，六个生命周期步骤全部通过。崩溃恢复由同一回归中的 GUI/Daemon/Runtime Kill Matrix 验证；回滚由 Release Manager 与 Database 双层 Schema 降级阻断验证。

正式发布仍要求 Authenticode：未签名 RC 是工程验证 Artifact，不可发布；受控发布环境必须注入证书，且 `forceCodeSigning` 会拒绝无证书构建。

结果：通过。

## T5.5-C5 第 37 章审计证据

`v1-acceptance-evidence.json` 覆盖第 37.1–37.8 的 8 个分类、66 个验收项。测试解析架构文档中的每一项任务映射，确认所有 T/G 编号都有至少一个存在的 ADR、Spike 或 Gate 报告。第 37.0 总门槛不在 T5.5 提前勾选，必须等待 G5。

结果：通过。

## 验证命令

```powershell
pnpm install
npm run test:contract
npm run test:git
npm run test:recovery
npm run test:security
npm run test:alpha
npm run test:dual-runtime
npm run test:release
npm run test:release-candidate
npm run test:host
.\scripts\verify-windows-release-candidate.ps1
pnpm run typecheck
npm run check
git diff --check
```

## 最终验证结果

- Workspace Build：20/20 包通过；TypeScript typecheck：32/32；
- T5.5 专项：4/4；要求的 V1 回归测试全部通过；
- 本机 Windows x64 RC 生命周期：6/6；
- 第 37.1–37.8：66/66 项存在审计证据映射；
- Checkpoint 校验：27 tasks、6 gates、3 backlog，状态 valid；
- Secret Scan：241 个文件，状态 clean；`git diff --check`：通过。

## 结论与边界

- T5.5：通过。
- 只有本提交 Windows CI 成功后才允许开始 G5。
- 未签名工程 Artifact 不构成公开 Release；正式发布必须由受控环境完成 Authenticode 与 Ed25519 Release Manifest 双重签名。
- B1–B3 未计入 V1 完成率，Claude、Generic ACP、WSL、macOS 和 Linux 未包装成已发布能力。